<%= form_for(@projekt) do |f| %>
  <% if @projekt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@projekt.errors.count, "error") %> prohibited this projekt from being saved:</h2>

      <ul>
      <% @projekt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  

  <div class="field">
    <%= f.label :Projektname %>
    <%= f.text_field :projektname, :id=>'projektname' %>
  </div>
  <div class="field">
    <%= f.label :Projektleiter %>
    <%= f.text_field :projektleiter, :value => current_user.email, :readonly => true %>
  </div>
  <div class="field">
    <%= f.label :Projektstart %>
    <%= f.date_select :projektstart , :id=>'projektstart'%>
  </div>
  <div class="field">
    <%= f.label :Projektbeschreibung %>
    <%= f.text_area:projektbeschreibung %>
  </div>
  <div class="field">
    <%= f.submit %>
  </div>
<% end %>
