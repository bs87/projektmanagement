<%= form_for(@ressourcefunction) do |f| %>
  <% if @ressourcefunction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ressourcefunction.errors.count, "error") %> prohibited this ressourcefunction from being saved:</h2>

      <ul>
      <% @ressourcefunction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.submit %>
  </div>
<% end %>
