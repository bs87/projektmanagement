<%= form_for(@produkt) do |f| %>
  <% if @produkt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@produkt.errors.count, "error") %> prohibited this produkt from being saved:</h2>

      <ul>
      <% @produkt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :typ %><br>
    <%= f.text_field :typ, :value=>"Liefergegenstand", :readonly=>true %>
  </div>
  <div class="field">
    <%= f.label :kat_id %><br>
    <%= f.number_field :kat_id, :value=>@kat_id, :readonly=>true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
